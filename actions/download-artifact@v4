- name: Download test results
  uses: actions/download-artifact@v4
  with:
    name: test-results              # must match upload name
    path: ./results                 # where to put it in the runner
    